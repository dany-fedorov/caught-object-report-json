import {
  makeReport,
  makeReportArray,
  resolveCorjRedactPolicy,
  restoreExpectedValues,
} from 'caught-object-report-json';

export const removed = {
  makeReport,
  makeReportArray,
  resolveCorjRedactPolicy,
  restoreExpectedValues,
};
